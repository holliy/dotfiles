[helpers]

[default]
depends = ["vim", "bash", "neovim"]

[default.files]

[vim.files]
vimrc = { target="~/.vimrc", type="symbolic" }
gvimrc = { target="~/.gvimrc", type="symbolic" }
vim = { target="~/.vim", type="symbolic" }

[bash.files]
bashrc = "~/.bashrc"

[neovim.files]
nvimrc = { target="~/.config/nvim/init.vim", type="symbolic" }

[default.variables]
